@model bibliotecaModel.ModelRacas

@{
    if (Session["Funclogado"] != null)
    {
        Layout = "~/Views/Shared/_LayoutFunc.cshtml";
    }

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Excluir</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>ModelRacas</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.id_raca)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.id_raca)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.nome_raca)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.nome_raca)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ft_raca)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ft_raca)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</body>
</html>
